package version1;

public abstract class Atributo {

	//abstract public void limites(Instancia i);	// metodo para hallar los limites supp e inferior , se empleara en el constructor
												// y modificara los valores minimos y maximos de cada columna si aparece
												// un nuevo min o max , asique en el constructor se le llamara por cada fila del ArrayList
	/*
	abstract public void add(Double a);
	abstract public void add(String a);*/
}
